import React from 'react';
import DefaultLayout from '../components/DefaultLayout';

const Register = () => {
    return (
        <DefaultLayout>
            <h2>Register page</h2>
        </DefaultLayout>
    );
};

export default Register;